# Angular

- Para zerar o docker, execute: `angular\docker\zerar_docker.ps1`
- Para iniciar o docker, execute: `angular\docker\iniciar_docker.ps1`
- Para iniciar o projeto Angular, dentro do docker: `./execute_angular.sh`

O projeto deve executar em: `http://localhost:4200/`
